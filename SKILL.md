---
name: trail-image-generation-skill
description: 将 GPX、KML 或 KMZ 徒步/旅行轨迹转换为可核对的路线资料、打卡点方案和一致的生图提示词；先核对事实与点位，再执行图像生成。
---

# 轨迹生图 Skill

严格按以下门禁执行，不得合并、跳过或提前进入下一阶段：

1. 按 `references/review-template.md` 创建并写入 `<路线名称>-路线核对单.md`，输出基础信息后等待用户回复 `基础信息确认无误`。聊天消息不能替代核对单文件。
2. 把打卡点和标志性主照片写入同一份核对单，展示更新后的对应章节，等待用户回复 `打卡点和标志性图片确认无误`。
2.5. 对需要 AI 转绘的地标建立独立简化计划：记录主体、1—3 个必须保留特征、禁止内容、裁切焦点、隐私处理和最终画面风格。先只读检查当前会话实际附带的生图/编辑工具 schema 和本地 provider 配置，使用 `build_image_capability_report.py` 记录 `true / false / unknown` 能力，不发起生图请求；不得根据模型名称猜价格、质量、尺寸或参考图容量。把能力报告、地标规格和候选预绘执行器写入同一核对单并等待 `地标简化规格确认无误`。确认后才可展示中间生图方式和逐张原图上传清单；每次调用只能上传一张已确认原图。provider 明确支持尺寸与质量控制时，优先请求其已确认的较小方形尺寸和 low/fast/draft 档；工具不暴露控制参数时使用 provider 默认输出并如实记录原始尺寸，不声称省钱。为便于稳定拼板，原始稿不是较小方图时可用 Pillow 确定性生成独立的拼板标准稿，默认 512×512；标准稿不得冒充模型原始输出。不得自动接受或重试。若多个工具能力不明，可在单独授权后用一张已确认代表照片各生成一张选型小样，由用户指定预绘执行器；小样授权不扩展为批量授权。将逐张生成结果确定性组成编号审核板，完整展示后等待 `地标简化稿确认无误`。没有照片、用户选择文字描述、没有可用参考图工具或要求保留原图像素时可跳过本阶段，并在核对单中记录原因。
3. 将已确认起点、终点名称写入点位规格，使用 `route_to_svg.py --checkpoints-json` 生成带起终点名称、起终点角色和全局编号点位的 SVG 与布局 JSON，再使用 `render_route_preview.py` 生成保留同一起终点标记的 PNG 路线预览；SVG 必须显示完整起终点名称，PNG 必须至少清晰显示起点/终点角色，并在字体能力可用时显示完整名称。检查路线、方向、起终点和点位锚点，并把三项几何资产的实际路径记录到同一份核对单。
4. 把完整的图片生成计划和画面规格实际写入同一份 `<路线名称>-路线核对单.md`，不得创建脱离前文的单独规格文档，也不得只在聊天中展示。强制填写并确认四个项目：`画面风格`、`生图数量`、`图中文字`、`轨迹精度`。画面风格必须沿用已确认地标简化稿；如需更改，先使相关简化稿和参考板失效并返回步骤 2.5。生图数量默认 `1 张`；轨迹精度默认 `精确轨迹/严格遵循轨迹`；其他项目必须给出具体值或明确写“无文字/由后期添加”。除非用户明确授权，不得降级为“结构相似”或“氛围自由”。每张图片还必须记录 `背景环境` 和 `地标连接方式`：默认采用干净无连线构图，通过相同编号、名称和邻近排版对应；地域背景只有在用户确认后才加入，并保持低对比P1远景。每个图片任务从已确认简化稿中选择最多 12 张，使用 `build_landmark_reference_board.py` 生成自己的地标参考板。写完后必须重新读取文件，向用户提供文件路径并完整展示第 4 阶段内容。
4.5. 在进入步骤 5 前执行一次路线文案调研。使用联网搜索查询当前路线的可靠资料，重点覆盖：`适合时间/季节`、`适合人群`、`不适合人群`、`装备建议`、`天气与路况风险`、`补给与饮水`、`交通或停车`、`通行限制/开放状态`、`环保与安全注意事项`。优先使用景区、政府、保护区、气象、交通等官方来源；再用可靠的本地户外或旅行资料补充体验信息。将来源标题、链接、发布日期或查询日期、支持的结论和可信度写入同一份核对单，区分“轨迹事实 / 用户确认 / 网络来源 / 创意建议”。从中筛选真正有用、不过度冗长的内容，更新每张图片的文字清单和文案信息；来源网址默认不作为画面文字。找不到可靠资料时明确写“未找到可确认资料”，不得编造。全部写入后重新读取核对单并运行 `validate_stage4_review.py`；校验通过才展示调研结果和拟采用文案，等待用户回复 `路线文案与注意事项确认无误`。
5. 输出“生图所用基础信息摘要”，明确列出路线事实、每日分段、全局点位、图像任务、视觉规则、SVG、文案、安全和补给信息。
6. 等待用户回复 `生图画面规格确认无误`。
7. 同时读取 `references/prompt-rules.md` 和 `references/geometry-contract.md`，为每张图片生成一份独立、完整、可直接调用的最终提示词；此时不得调用最终路线图生图工具。步骤 2.5 已授权的地标简化调用不受本句影响。提示词必须保留不可改写的几何锁定区和文字锁定区，明确 `点位完整性`、`点位对应`、`地标连接方式`，并完整列出 `实际上传参考图`、`地标参考板编号映射`、`素材事实来源（不上传）`、`校验资产（不上传）`、`单图兼容分支`、负面约束和验收清单。选择后期添加文字时仍必须逐项列出全部已确认文字及对应排版区域，不得用“标题、点位等”概括或省略清单。不得使用“同上”“沿用共享设置”“参考前文”、省略号或只交付提示词摘要。
8. 将每张最终提示词的完整正文实际展示给用户核对，并等待用户回复 `生图提示词确认无误`。用户提出修改时返回第 7 步，重新展示修改后的完整提示词并再次确认。没有收到该确认短语，不得展示执行授权选项、生成执行清单或调用生图工具。
9. 提示词确认后展示生成方式选择；用户明确授权生成后，先生成执行清单并运行 `validate_generation_manifest.py`，再从已确认提示词文件原样读取调用，不得手工缩写、拼接或重新概括提示词。只有校验通过才调用当前会话可用的生图或编辑工具。

用户选择提示词模式并回复 `全部确认，只生成提示词` 时，也必须先完成第 8 步的完整提示词核对；确认后停止，不调用生图工具。

第 4 阶段文档未实际写入、未通过 `validate_stage4_review.py`、四个项目没有全部确认，或尚未收到 `路线文案与注意事项确认无误` 时，停止在画面规格与文案调研阶段：不得输出最终基础信息摘要、不得生成独立提示词、不得生图。

只有收到 `生图提示词确认无误` 后，才重新执行一次只读能力发现并展示以下选择，不根据模型名称自动判断强弱，也不沿用地标预绘授权：

1. `使用当前会话生图`：实际调用当前会话提供的生图工具，不自行连接外部接口。只有工具明确暴露模型名称、尺寸和质量档位时才记录具体值；否则模型写 `tool-managed/unknown`，相关能力写 `unknown`。
2. `使用已配置中转站`：读取用户本地 provider 配置；调用前展示提供方、目标地址、模型、接口格式和将上传的素材，等待用户明确确认。
3. `只保留提示词`：不调用生图工具，供用户复制到其他模型或稍后使用。

选定提供方后，使用 `直接生成完整效果图`。最终路线图执行器必须单独标记为 `route_final`；只有 provider 声明或用户通过结果确认其适合终稿时才能称为高质量，能力未知时只能称为“可执行但质量档位未知”。直接完整生图不会自动降低轨迹精度，必须执行几何锁定和生成前校验；本 Skill 不使用 SVG 后期确定性叠加来补救错误轨迹。

用户没有明确选择时停止在提示词交付阶段，不默认执行任何一种生成方式。

## 可配置图像提供方

需要选择图像执行器时读取 `references/provider-config.md`。先检查本轮实际可用工具 schema，再使用 `scripts/build_image_capability_report.py` 保存证据支持的能力报告；需要中转站时使用 `scripts/provider_config.py` 校验配置、按 `landmark_prepaint` 或 `route_final` 选择并输出脱敏摘要。不得把 Token 写入 Skill、配置样例、日志、提示词或 Git 仓库；配置只保存环境变量名称。不得未经确认上传路线 PNG、照片或提示词，也不得在中转站失败时静默切换其他提供方。

核心脚本：`inspect_kmz.py`、`route_to_svg.py`、`render_route_preview.py`、`detect_checkpoint_candidates.py`、`route_intensity.py`、`build_image_capability_report.py`、`provider_config.py`、`normalize_landmark_draft.py`、`build_landmark_generation_manifest.py`、`validate_landmark_generation_manifest.py`、`build_landmark_reference_board.py`、`validate_stage4_review.py`、`build_generation_manifest.py`、`validate_generation_manifest.py`。

## 核对单持久化合同

- `<路线名称>-路线核对单.md` 是阶段 1—6 的唯一事实来源；各阶段必须更新同一文件，不能用聊天摘要代替。
- 第 4 阶段写入前先读取现有文件，保留用户已经确认的基础信息、点位修正、照片选择和文案，不得重新生成覆盖。
- 第 4 阶段必须填满图片任务、逐图规格、视觉与轨迹规则、点位视觉来源、图片文字、几何资产和基础信息摘要；不得留下 `<占位符>`、空单元格或“后续补充”。
- 步骤 4.5 的网络调研必须写入同一核对单，至少处理适合时间、适合/不适合人群和注意事项；每条结论都要有来源或明确标记为用户确认，不能把搜索摘要直接当作事实。
- 网络调研内容要经过筛选：核对单保留来源和完整依据，图片文字只保留短句式重点；动态通行、天气和施工信息必须注明查询日期并提醒出发前再次核实。
- 写入后重新读取文件，确认章节存在且内容与本轮展示一致，再运行 `validate_stage4_review.py`。
- 对用户展示核对单的可点击路径和第 4 阶段完整正文。只有文件内容通过校验，才请求 `生图画面规格确认无误`。

## 强制参考层与优先级

- SVG 路线骨架、PNG 路线预览、布局 JSON 和其全部编号标注点是最终生图的必备几何资产，必须可见、可核对、不得重排。
- SVG、PNG 与布局 JSON 必须共同记录起点和终点。开放路线分别显示绿色起点与红色终点；闭合路线显示“起点/终点”合并标记。不得只依赖最终生图提示词补充起终点。
- 轨迹、锚点、方向、起终点、分段和 inset 为 P0；文案、信息卡、字体、地标布局、背景和装饰为 P1。冲突时 P1 必须避让 P0。
- 生图模型优先使用 PNG 作为最高优先级视觉几何参考；SVG 用于核对和可选编辑，布局 JSON 用于校验锚点。不得假设模型一定支持 SVG，也不采用 SVG 后期叠加来替代生图前的几何约束。
- 最终提示词必须保留路线和编号点位；只能改变线宽、颜色和画风，不能遗漏、增补、镜像、旋转或移动标注点。
- 每个应显示点位必须同时满足：轨迹锚点存在、编号正确、名称正确、地标身份正确且一一对应。地标卡片存在不能替代轨迹锚点。地标与轨迹默认不使用引导连线，通过相同编号、名称和邻近布局建立对应；只有用户明确要求连线时才允许启用，且不得错连、交叉误导或为了连线移动 P0 锚点。
- 地域背景属于 P1。只有用户确认具体视角或环境时才加入；使用低对比、空气透视和远景层级，不得遮挡或降低路线、起终点、编号锚点、文字和地标的可读性，也不得把未经确认的具体建筑或动态城市状态画成事实。
- 生成后必须验收路线形状、方向、起终点、分段边界和编号锚点；不合格不得保存为正式版本，允许一次针对性重试。
- 默认不自动生成示意等高线，也不把等高线作为提示词或生图门禁。只有用户提供可靠地形数据并明确要求时，才单独规划地形图层。

## 地标预绘与两图输入合同

- 原始照片、用户确认特征和核对单是地标事实来源；AI 简化稿只是 P1 视觉素材，冲突时以事实来源为准。
- 地标简化稿使用 provider 已确认的较小方形尺寸和低成本/快速档；原始输出尺寸必须记录，不强制假装为 512×512。用于审核板和最终参考板的标准稿默认 512×512，可由原始稿确定性归一化生成。画面保持浅色纯净背景、主体居中、无人物、无广告、无点位编号。原图中真实存在且已确认的牌匾、石刻和地标名称允许参考保留；不得新增原图不存在的招牌、名称或随机文字。不得把多张原图先拼板再交给模型重绘。每次生成只上传一张原图。
- 地标预绘执行器必须标记为 `landmark_prepaint`，最终执行器必须标记为 `route_final`；两者分别选择、分别授权。地标小样、批量预绘和最终路线图是三个不同授权范围。
- 当前会话工具隐藏模型、尺寸、质量或价格时必须记录 `tool-managed/unknown` 或 `unknown`；能执行不等于便宜或高质量。中间生图必须单独确认 provider、模型或未知状态、原图上传路径和隐私处理。中转站必须支持图片参考或图片编辑；失败时不得静默切换。
- 审核板每张最多 12 格；超过 12 张时可生成多张审核板。修改任一简化稿后，`地标简化稿确认无误` 失效。
- 最终每个路线图任务最多上传两张图：第一张始终是 `route_geometry` 轨迹 PNG；第二张可选且只能是 `landmark_reference_board`。SVG、布局 JSON、原始照片和单张简化稿不得进入最终上传列表。
- provider 只能接收一张参考图时，必须展示并确认单图兼容执行清单，只上传轨迹 PNG，地标改用已确认文字特征；不得静默遗漏参考板。
- 没有能力明确的低成本预绘工具时，可选择一张代表照片做用户授权的小样选型，或只生成逐张预绘提示词；不得把价格未知的工具标成“便宜模型”。没有明确的终稿质量证据时，可执行但必须标记“质量档位未知”。
- 用户要求保留原照片像素时，简化稿不能替代原图；路线底图生成后另做确定性照片合成。

## 多日徒步增强规则

当轨迹跨越两天或以上时，额外建立“天数—路段—跨夜点”三层结构：

- 保留一套全局打卡点编号；日详图只能引用全局编号，不得从 1 重新编号。
- 明确每天的起点、终点、轨迹进度、距离、爬升、下降、最高点、补给和住宿/露营状态。
- 将跨夜节点作为正式路线节点，记录“第 N 天结束 / 第 N+1 天起点”，不能只写在备注里。
- 同时生成一张总览图和每天的独立详图；所有图片共享同一条几何路线、方向、点位名称和颜色系统。
- 每日详图可突出当天路线，其余路线作为低对比度 inset；总览图显示完整路线和跨夜关系。
- 对补给、饮水、住宿、撤离点、危险路段设置独立信息卡，并标明“轨迹事实 / 用户确认 / 创意表现”。
- 若多日轨迹首尾相连，仍按真实行进方向绘制闭环；若不闭合，不得为了构图补成闭环。
- 计算每日统计时保留分段依据；源文件统计与计算候选有差异时，两者并列展示。

推荐多日执行顺序：

1. 先锁定全局路线和天数切分。
2. 再确认每日终点、跨夜点、补给和住宿。
3. 再确认全局打卡点及主照片。
4. 对需要 AI 转绘的主照片逐张生成并确认地标简化稿与审核板。
5. 生成并核对 SVG 标注点与 PNG 路线预览。
6. 输出总览图与每日详图计划、各自地标参考板、画面规格和生图基础信息摘要。
7. 用户确认画面规格后，先生成总览提示词，再生成每日详图提示词。
8. 获得最终生图授权后，再按已确认的生成节奏执行。
