---
name: trail-image-generation-skill
description: 将 GPX、KML 或 KMZ 徒步/旅行轨迹转换为可核对的路线资料、打卡点方案和一致的生图提示词；先核对事实与点位，再执行图像生成。
---

# 轨迹生图 Skill

严格按以下门禁执行，不得合并、跳过或提前进入下一阶段：

1. 按 `references/review-template.md` 输出基础信息，等待用户回复 `基础信息确认无误`。
2. 输出打卡点和标志性主照片，等待用户回复 `打卡点和标志性图片确认无误`。
3. 使用 `route_to_svg.py --checkpoints-json` 生成带全局编号点位的 SVG 和布局 JSON，再使用 `render_route_preview.py` 生成 PNG 路线预览；检查路线、方向和点位锚点。
4. 把图片生成计划和画面规格追加到同一份核对单，并强制确认三个项目：`画面风格`、`生图数量`、`图中文字`。生图数量默认 `1 张`；另外两项必须展示具体内容或明确写“无文字/由后期添加”，不得留空或由 Agent 静默决定。
5. 输出“生图所用基础信息摘要”，明确列出路线事实、每日分段、全局点位、图像任务、视觉规则、SVG、文案、安全和补给信息。
6. 等待用户回复 `生图画面规格确认无误`。
7. 按 `references/prompt-rules.md` 为每张图片生成一份独立提示词；此时仍不得调用生图工具。
8. 提示词交付后展示生成方式选择；只有用户明确选择一种方式后，才调用当前会话可用的生图或编辑工具。

用户选择提示词模式并回复 `全部确认，只生成提示词` 时，在第 7 步交付提示词后停止。

第 4 阶段的三个项目没有全部确认时，停止在画面规格阶段：不得输出最终基础信息摘要、不得生成独立提示词、不得生图。

提示词完成后必须展示以下选择，不根据模型名称自动判断强弱：

1. `使用当前会话生图`：默认请求模型 `gpt-image-2`；实际调用当前会话提供的生图工具，不自行连接外部接口。
2. `使用已配置中转站`：读取用户本地 provider 配置；调用前展示提供方、目标地址、模型、接口格式和将上传的素材，等待用户明确确认。
3. `只保留提示词`：不调用生图工具，供用户复制到其他模型或稍后使用。

选定提供方后，再选择 `直接生成完整效果图` 或 `生成视觉底图并后期叠加`。默认模型为 `gpt-image-2`，但允许在 provider 配置中覆盖。

用户没有明确选择时停止在提示词交付阶段，不默认执行任何一种生成方式。

## 可配置图像提供方

需要中转站时读取 `references/provider-config.md`。使用 `scripts/provider_config.py` 校验配置并输出脱敏摘要。不得把 Token 写入 Skill、配置样例、日志、提示词或 Git 仓库；配置只保存环境变量名称。不得未经确认上传路线 PNG、照片或提示词，也不得在中转站失败时静默切换其他提供方。

核心脚本：`inspect_kmz.py`、`route_to_svg.py`、`render_route_preview.py`、`detect_checkpoint_candidates.py`、`route_intensity.py`。

## 强制参考层

- SVG 路线骨架、PNG 路线预览、布局 JSON 和其全部编号标注点是最终生图的必备几何资产，必须可见、可核对、不得重排。
- 生图模型优先使用 PNG 作为视觉参考；SVG 用于编辑和最终确定性叠加，布局 JSON 用于核对锚点。不得假设模型一定支持 SVG。
- 最终提示词必须保留路线和编号点位；只能改变线宽、颜色和画风，不能遗漏、增补、镜像、旋转或移动标注点。
- 默认不自动生成示意等高线，也不把等高线作为提示词或生图门禁。只有用户提供可靠地形数据并明确要求时，才单独规划地形图层。

## 多日徒步增强规则

当轨迹跨越两天或以上时，额外建立“天数—路段—跨夜点”三层结构：

- 保留一套全局打卡点编号；日详图只能引用全局编号，不得从 1 重新编号。
- 明确每天的起点、终点、轨迹进度、距离、爬升、下降、最高点、补给和住宿/露营状态。
- 将跨夜节点作为正式路线节点，记录“第 N 天结束 / 第 N+1 天起点”，不能只写在备注里。
- 同时生成一张总览图和每天的独立详图；所有图片共享同一条几何路线、方向、点位名称和颜色系统。
- 每日详图可突出当天路线，其余路线作为低对比度 inset；总览图显示完整路线和跨夜关系。
- 对补给、饮水、住宿、撤离点、危险路段设置独立信息卡，并标明“轨迹事实 / 用户确认 / 创意表现”。
- 若多日轨迹首尾相连，仍按真实行进方向绘制闭环；若不闭合，不得为了构图补成闭环。
- 计算每日统计时保留分段依据；源文件统计与计算候选有差异时，两者并列展示。

推荐多日执行顺序：

1. 先锁定全局路线和天数切分。
2. 再确认每日终点、跨夜点、补给和住宿。
3. 再确认全局打卡点及主照片。
4. 生成并核对 SVG 标注点与 PNG 路线预览。
5. 输出总览图与每日详图计划、画面规格和生图基础信息摘要。
6. 用户确认画面规格后，先生成总览提示词，再生成每日详图提示词。
7. 获得最终生图授权后，再按已确认的生成节奏执行。
